# Kagent Configuration Template
# Copy this file to .env and fill in your actual values

# OpenAI API Configuration (Required)
OPENAI_API_KEY="sk-your-openai-api-key-here"

# Kind cluster name (optional)
KIND_CLUSTER_NAME="kagent-demo"

# Custom CA certificate bundle path (optional)
# Set this if you need to handle corporate certificates or custom CA bundles
# CA_BUNDLE_PATH="/path/to/your/ca-bundle.crt"

# Kubernetes Configuration (these are defaults, don't change unless needed)
ARGOCD_NAMESPACE="argocd"
KAGENT_NAMESPACE="kagent"